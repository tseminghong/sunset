<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Notification permissions for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Alarm permissions for exact scheduling -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    
    <!-- Wake lock for alarms -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Foreground service for notifications -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    
    <!-- ColorOS specific permissions for live alerts -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sunset"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name="com.sunset.ictstudy.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Sunset"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Reminder broadcast receiver -->
        <receiver
            android:name="com.sunset.ictstudy.notifications.StudyReminderReceiver"
            android:enabled="true"
            android:exported="false" />
        
        <!-- Live Alert action receiver for timer controls -->
        <receiver
            android:name="com.sunset.ictstudy.notifications.LiveAlertActionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.sunset.ictstudy.ACTION_PAUSE_TIMER" />
                <action android:name="com.sunset.ictstudy.ACTION_RESUME_TIMER" />
                <action android:name="com.sunset.ictstudy.ACTION_STOP_TIMER" />
            </intent-filter>
        </receiver>
        
        <!-- Firebase Cloud Messaging service for Live Alert updates -->
        <service
            android:name="com.sunset.ictstudy.notifications.LiveAlertFCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>
